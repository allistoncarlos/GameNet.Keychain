import XCTest
@testable import GameNet_Keychain

final class GameNet_KeychainTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GameNet_Keychain().text, "Hello, World!")
    }
}
